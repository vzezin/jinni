# Data

The evaluation data sets behind the manual are provided on request; write to the authors.

Cases are not published here on purpose: public files are scraped into training corpora, and a data set that has entered a model can no longer test that model. The evaluation cases would stop being reusable on the models that release next, losing comparative power -- or requiring massive spend on corpus re-runs for every model scored for manual maintenance.
